<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      * {
        font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
          Roboto, Oxygen, Ubuntu, Cantarell, "Open Sans", "Helvetica Neue",
          sans-serif;
      }

      h1 {
        color: #d37676;
        font-weight: 500;
        font-size: 40px;
      }
      p {
        color: #358f08;
        font-weight: 300;
        font-size: 32px;
      }
      b {
        color: #679ba0;
      }
    </style>
  </head>
  <body>
    <h1>Hi, Welcome to THE SERVER</h1>
    <p>
      Here are some of the work ideas <br />
      add <b>/user/register</b> to url to register as a new USER
    </p>

    <h1>Users List</h1>
  <table border="1">
    <thead>
      <tr>
        <th>ID</th>
        <th>User Name</th>
        <th>Email</th>
      </tr>
    </thead>
    <tbody>
      <% users.forEach(users => { %>
        <tr>
          <td><%= users.id %></td>
          <td><%= users.username %></td>
          <td><%= users.email %></td>
        </tr>
        <% }) %>
    </tbody>
  </table>
    </ul>
  </body>
</html>
